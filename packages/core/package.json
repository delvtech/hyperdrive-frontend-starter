{
  "name": "@starter-repo/core",
  "description": "Example core library for interacting with Hyperdrive",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --minify --format esm --clean --dts",
    "watch": "npm run build -- --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@starter-repo/eslint-config": "*",
    "@starter-repo/prettier-config": "*",
    "@starter-repo/tsconfig": "*",
    "dotenv": "^16.4.5",
    "prettier-plugin-organize-imports": "^3.2.4",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "tsup": "^7.2.0",
    "typescript": "^5.2.2",
    "vite-tsconfig-paths": "^4.2.1",
    "vitest": "^0.34.6"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {}
}
